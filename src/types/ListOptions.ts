export default interface ListOptions {
    beforeId?: string;
    beforeValue?: string;
    afterId?: string;
    afterValue?: string;
    sortBy?: string;
    sortOrder?: string;
    page?: number;
    limit?: number;
}
